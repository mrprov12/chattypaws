# Chattypaws — copy to .env and fill in.
# Backend reads from repo root .env (see backend/config.py).

# Tapo C120 RTSP (Camera Account user/password from Tapo app).
# Format: rtsp://USER:PASSWORD@CAMERA_IP:554/stream1
RTSP_URL=rtsp://user:password@192.168.1.100:554/stream1

# PostgreSQL for events and (later) mappings, push subscriptions.
DATABASE_URL=postgresql://localhost/chattypaws
