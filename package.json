{
  "name": "chattypaws",
  "private": true,
  "description": "Monorepo root — commit linting and hooks only",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
